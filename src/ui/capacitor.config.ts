import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.playpokerodds.app',
  appName: 'playpokerodds',
  webDir: 'dist/apps/client',
  bundledWebRuntime: false
};

export default config;
